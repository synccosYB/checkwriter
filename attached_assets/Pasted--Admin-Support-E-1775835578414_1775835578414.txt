/* ─────────────────────────────────────────────────────────────────────────────
   Admin Support Email — Component Styles
   Add to your global stylesheet or a scoped CSS/module file.
───────────────────────────────────────────────────────────────────────────── */

/* Shared overlay */
.modal-overlay,
.drawer-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0, 0, 0, 0.45);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 1000;
}

/* ── Modal ── */
.modal-container {
  background: #fff;
  border-radius: 10px;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.16);
  width: 100%;
  max-width: 560px;
  max-height: 90vh;
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20px 24px 16px;
  border-bottom: 1px solid #e5e7eb;
}

.modal-title {
  font-size: 18px;
  font-weight: 600;
  color: #111827;
  margin: 0;
}

.modal-close-btn {
  background: none;
  border: none;
  font-size: 16px;
  cursor: pointer;
  color: #6b7280;
  padding: 4px 8px;
  border-radius: 4px;
  line-height: 1;
}
.modal-close-btn:hover { background: #f3f4f6; color: #111827; }

.modal-body {
  padding: 20px 24px;
  overflow-y: auto;
  flex: 1;
}

.modal-footer {
  display: flex;
  justify-content: flex-end;
  gap: 10px;
  padding: 16px 24px;
  border-top: 1px solid #e5e7eb;
}

/* ── Form fields ── */
.form-field { margin-bottom: 16px; }

.form-label {
  display: block;
  font-size: 13px;
  font-weight: 500;
  color: #374151;
  margin-bottom: 6px;
}

.required { color: #ef4444; margin-left: 2px; }

.form-input,
.form-textarea {
  width: 100%;
  padding: 9px 12px;
  border: 1px solid #d1d5db;
  border-radius: 6px;
  font-size: 14px;
  color: #111827;
  background: #fff;
  transition: border-color 0.15s;
  box-sizing: border-box;
}
.form-input:focus,
.form-textarea:focus {
  outline: none;
  border-color: #6366f1;
  box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.1);
}
.form-input--disabled,
.form-input:disabled { background: #f9fafb; color: #6b7280; cursor: not-allowed; }
.form-textarea { resize: vertical; min-height: 120px; font-family: inherit; }

.form-error {
  color: #dc2626;
  font-size: 13px;
  margin-top: 8px;
  padding: 8px 12px;
  background: #fef2f2;
  border-radius: 6px;
}

/* ── Success state ── */
.email-success {
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 32px 0;
  text-align: center;
  color: #374151;
}
.email-success-icon {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 52px;
  height: 52px;
  border-radius: 50%;
  background: #d1fae5;
  color: #059669;
  font-size: 24px;
  margin-bottom: 16px;
}

/* ── Buttons ── */
.btn {
  padding: 8px 18px;
  border-radius: 6px;
  font-size: 14px;
  font-weight: 500;
  cursor: pointer;
  border: none;
  transition: background 0.15s, opacity 0.15s;
}
.btn:disabled { opacity: 0.5; cursor: not-allowed; }
.btn--primary { background: #6366f1; color: #fff; }
.btn--primary:hover:not(:disabled) { background: #4f46e5; }
.btn--secondary { background: #f3f4f6; color: #374151; border: 1px solid #d1d5db; }
.btn--secondary:hover:not(:disabled) { background: #e5e7eb; }
.btn--ghost { background: transparent; color: #6366f1; border: 1px solid #e5e7eb; }
.btn--ghost:hover:not(:disabled) { background: #f5f3ff; }
.btn--sm { padding: 6px 12px; font-size: 13px; }

/* ── Drawer ── */
.drawer-overlay { justify-content: flex-end; }

.drawer-panel {
  background: #fff;
  height: 100%;
  width: 100%;
  max-width: 460px;
  box-shadow: -4px 0 24px rgba(0,0,0,0.12);
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.drawer-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  padding: 20px 24px 16px;
  border-bottom: 1px solid #e5e7eb;
  flex-shrink: 0;
}

.drawer-title { font-size: 18px; font-weight: 600; color: #111827; margin: 0 0 2px; }
.drawer-subtitle { font-size: 13px; color: #6b7280; margin: 0; }

.drawer-body {
  flex: 1;
  overflow-y: auto;
  padding: 16px 24px;
}

.drawer-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 14px 24px;
  border-top: 1px solid #e5e7eb;
  flex-shrink: 0;
}

.drawer-loading,
.drawer-empty,
.drawer-error {
  text-align: center;
  color: #9ca3af;
  font-size: 14px;
  padding: 40px 0;
}
.drawer-error { color: #dc2626; }

.link-btn {
  background: none;
  border: none;
  color: #6366f1;
  cursor: pointer;
  font-size: inherit;
  padding: 0;
  text-decoration: underline;
}

.pagination-label { font-size: 13px; color: #6b7280; }

/* ── Email history list ── */
.email-history-list {
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.email-history-item {
  border: 1px solid #e5e7eb;
  border-radius: 8px;
  overflow: hidden;
  transition: border-color 0.15s;
}
.email-history-item:hover { border-color: #c7d2fe; }
.email-history-item.is-failed { border-color: #fca5a5; background: #fff7f7; }

.email-history-item__header {
  width: 100%;
  background: none;
  border: none;
  text-align: left;
  padding: 12px 14px;
  cursor: pointer;
  display: grid;
  grid-template-columns: 1fr auto;
  grid-template-rows: auto auto;
  gap: 2px 8px;
  align-items: start;
}
.email-history-item__header:hover { background: #f9fafb; }

.email-history-item__meta {
  display: flex;
  align-items: center;
  gap: 8px;
  grid-column: 1;
  grid-row: 1;
}

.email-history-item__subject {
  font-size: 14px;
  font-weight: 500;
  color: #111827;
}

.email-history-item__info {
  display: flex;
  gap: 12px;
  font-size: 12px;
  color: #6b7280;
  grid-column: 1;
  grid-row: 2;
}

.email-history-item__chevron {
  grid-column: 2;
  grid-row: 1 / 3;
  align-self: center;
  font-size: 10px;
  color: #9ca3af;
}

.email-history-item__body {
  padding: 12px 14px;
  border-top: 1px solid #f3f4f6;
  background: #f9fafb;
}
.email-history-item__body-text {
  font-size: 14px;
  color: #374151;
  white-space: pre-wrap;
  margin: 0 0 10px;
  line-height: 1.6;
}
.email-history-item__body-to {
  font-size: 12px;
  color: #6b7280;
  margin: 0;
}

/* Badge */
.badge {
  display: inline-block;
  padding: 2px 7px;
  border-radius: 999px;
  font-size: 11px;
  font-weight: 500;
}
.badge--error { background: #fee2e2; color: #b91c1c; }
